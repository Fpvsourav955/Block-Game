class AnimationConfig {
  static const Duration buttonPress = Duration(milliseconds: 100);
  static const Duration pieceSelection = Duration(milliseconds: 150);
  static const Duration piecePlacement = Duration(milliseconds: 180);
  static const Duration pieceReturn = Duration(milliseconds: 220);
  static const Duration lineAnticipation = Duration(milliseconds: 120);
  static const Duration lineClearing = Duration(milliseconds: 320);
  static const Duration comboDisplay = Duration(milliseconds: 600);
  static const Duration newRecordSequence = Duration(milliseconds: 1400);
  static const Duration screenTransition = Duration(milliseconds: 260);
  static const Duration scoreCountUp = Duration(milliseconds: 800);
}
